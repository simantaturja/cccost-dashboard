{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Cost Dashboard Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/extension"],
      "preLaunchTask": "build-extension"
    }
  ]
}
