{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-extension",
      "type": "shell",
      "command": "npm --prefix web run build && npm --prefix extension run build",
      "problemMatcher": [],
      "presentation": { "reveal": "silent" }
    }
  ]
}
