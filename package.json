{
  "name": "claude-code-cost-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix web run build",
    "test": "node --test"
  }
}
